{
  "name": "anava-local-connector-tests",
  "version": "1.0.0",
  "description": "Test suite for Anava Local Connector",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.2.0",
    "@vitest/coverage-v8": "^1.2.0",
    "@vitest/ui": "^1.2.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
